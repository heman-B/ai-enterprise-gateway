# LLM Gateway — Python-Abhängigkeiten
# Minimale Versionsgrenzen statt exakter Pins (FastMCP benötigt neuere Versionen)

# Web-Framework
fastapi>=0.115.6
uvicorn[standard]>=0.34.0

# Async HTTP-Client für Provider-Aufrufe
httpx>=0.28.1

# Circuit-Breaker und Retry-Logik
tenacity>=9.0.0

# Token-Zählung (OpenAI-kompatibel)
tiktoken>=0.8.0

# Rate-Limiting via Redis
redis>=5.2.1

# Database drivers (SQLite for dev, PostgreSQL for prod)
aiosqlite>=0.21.0
asyncpg>=0.30.0

# Datenvalidierung
pydantic>=2.11.7,<3

# MCP-Server (Agent-Interface für Claude, Cursor, SAP Joule)
fastmcp>=2.14,<3

# Umgebungsvariablen
python-dotenv>=1.1.0

# PII-Erkennung: Checksummen-Validierung
python-stdnum>=1.20

# Monitoring: Prometheus-Metriken
prometheus-client>=0.21.0

# Test-Abhängigkeiten
pytest>=8.3.4
pytest-asyncio>=0.24.0
respx>=0.21.1
