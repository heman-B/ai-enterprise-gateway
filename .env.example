# LLM Gateway — Umgebungsvariablen
# Kopieren und anpassen: cp .env.example .env

# ─── Admin-Zugriff (Bootstrap) ──────────────────────────────────────────────
# ⚠️ SICHERHEIT: Dieser Schlüssel umgeht die Datenbank-Prüfung!
# - Nur für initiale API-Key-Erstellung verwenden
# - In Produktion: Entfernen nach Bootstrap oder auf sicheres Secret setzen
# - Niemals in Git committen (steht in .gitignore)
ADMIN_API_KEY=my-super-secret-admin-key-123

# ─── LLM-Anbieter-Schlüssel ─────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-api03-...
OPENAI_API_KEY=sk-...
GEMINI_API_KEY=AIza...

# ─── Lokales LLM (Ollama) ───────────────────────────────────────────────────
OLLAMA_URL=http://host.docker.internal:11434

# ─── Infrastruktur ──────────────────────────────────────────────────────────
REDIS_URL=redis://localhost:6379
DATABASE_URL=./gateway.db

# ─── Datenhaltung ───────────────────────────────────────────────────────────
# 'true': Nur EU-konforme Provider (Anthropic + Ollama) — für DSGVO-Konformität
# 'false': Alle Provider erlaubt (Standard)
EU_RESIDENCY_REQUIRED=false

# ─── Rate-Limiting ──────────────────────────────────────────────────────────
# Standard-Rate-Limit pro Mandant (überschreibbar per Mandant in Redis)
DEFAULT_RATE_LIMIT_PER_MINUTE=100
